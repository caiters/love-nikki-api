var letterGrade = "Please leave a grade: SS, S, A, B, or C";

var customValidationMsgs = {
  en: {
    custom: {
      selectedStylesNum: {
        min_value: "Please select 5 styles."
      },
      ratingElegance: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingMature: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingCool: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingCute: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingLively: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingGorgeous: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingPure: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingSexy: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingSimple: {
        regex: letterGrade,
        required: letterGrade
      },
      ratingWarm: {
        regex: letterGrade,
        required: letterGrade
      }
    }
  }
};
